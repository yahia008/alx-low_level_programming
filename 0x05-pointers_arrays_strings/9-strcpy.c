#include <stdio.h>
#include <string.h>
#include "main.h"
/**
 * _strcpy - copy string from one source to a destination
 * @dest: the destination passed
 * @src: the source passed
 * Return: The copy of the string
 */
char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}
